"use strict";
const vue = require("vue");
const config = require("./config");
function useRequestProvider(config$1) {
  vue.provide(config.USEREQUEST_GLOBAL_OPTIONS_PROVIDE_KEY, config$1);
}
module.exports = useRequestProvider;
