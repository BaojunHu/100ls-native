"use strict";
const utils = require("./utils");
function isOnline() {
  if (utils.canUseDom() && typeof navigator.onLine !== "undefined") {
    return navigator.onLine;
  }
  return true;
}
module.exports = isOnline;
