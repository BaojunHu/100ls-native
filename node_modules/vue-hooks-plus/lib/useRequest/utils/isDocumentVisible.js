"use strict";
const utils = require("./utils");
function isDocumentVisible() {
  if (utils.canUseDom()) {
    return document.visibilityState !== "hidden";
  }
  return true;
}
module.exports = isDocumentVisible;
