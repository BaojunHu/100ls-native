"use strict";
const vue = require("vue");
function useUpdate() {
  const update = vue.ref({});
  const setUpdate = () => {
    update.value = Object.assign({}, { ...update.value });
  };
  return {
    update: vue.readonly(update),
    setUpdate
  };
}
module.exports = useUpdate;
