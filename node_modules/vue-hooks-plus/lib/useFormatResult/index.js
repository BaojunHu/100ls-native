"use strict";
const vue = require("vue");
function useFormatResult(data, formatResultCallback) {
  const formatResultData = vue.computed(() => formatResultCallback(vue.unref(data)));
  return formatResultData;
}
module.exports = useFormatResult;
