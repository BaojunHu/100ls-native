import { provide } from "vue";
import { USEREQUEST_GLOBAL_OPTIONS_PROVIDE_KEY } from "./config";
function useRequestProvider(config) {
  provide(USEREQUEST_GLOBAL_OPTIONS_PROVIDE_KEY, config);
}
export {
  useRequestProvider as default
};
