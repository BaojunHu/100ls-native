import { Ref, createApp, VueElement } from 'vue';
export default function usePreview(md: Parameters<typeof createApp> | Ref<string> | string | VueElement): {
    container: Ref<Element | undefined, Element | undefined>;
};
