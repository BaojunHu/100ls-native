import { computed, unref } from "vue";
function useFormatResult(data, formatResultCallback) {
  const formatResultData = computed(() => formatResultCallback(unref(data)));
  return formatResultData;
}
export {
  useFormatResult as default
};
